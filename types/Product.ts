type Product = {
    id: string;
    productName: string;
    stockPrice: number;
    sellPrice: number;
    stock: number;
    imageUrl: string;
    lowStockThreshold: number;
}

export default Product;